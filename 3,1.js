/*There is a function "addNumbers" already declared, it is 
receiving 2 parameters (the variables a and b),  as a developer 
you were given a task to fill the function content with the code 
needed to sum variable a with variable b and return the result 
of that operation.*/

function addNumbers(a,b)
{
	//your code here
	return a+b;
}

console.log(addNumbers(3,4));
